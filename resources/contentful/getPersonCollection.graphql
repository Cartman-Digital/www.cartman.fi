query personCollectionQuery(
  $list: Boolean = true,
  $preview: Boolean = false,
) {
  personCollection(
    preview: $preview
  ) {
    __typename
    items {
      __typename
      name
      email
      jobTitle
      office {
        name
      }
      slug
      picture {
        url
        title
      }
      __typename
      shortText @include(if: $list) {
        json
      }
      description @skip(if: $list) {
        json
      }
    }  
  }
}