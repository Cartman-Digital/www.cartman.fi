query previewPostQuery($id: String!) {
  post(preview: true, id: $id) {
    __typename
    author {
      name
      picture {
        url
      }
      email
    }
    type
    title
    publishDate
    slug
    content {
      json
    }
  }
}
